pfrom Tkinter import *
import webbrowser

root = Tk()

def weather():
	webbrowser.get('mozilla').open_new_tab('https://twojapogoda.pl/')

pogoda = Button(root, text="Pogoda", command=weather, padx=50, pady=50)
pogoda.grid(row=3,column=0)

root.mainloop()
